import {
  DEFAULT_LOCAL_PORT_CANDIDATES,
  buildLocalBaseUrl,
  getCurrentLocalPort,
  isLocalWorkbenchBaseUrl,
  normalizeLocalBaseUrl,
  probeWorkbenchBaseUrl,
  recoverLocalWorkbenchBaseUrl,
  waitForWorkbenchHealth,
} from '../shared/workbench-recovery.js';

export const __desktopApiTestables = {
  DEFAULT_LOCAL_PORT_CANDIDATES,
  buildLocalBaseUrl,
  getCurrentLocalPort,
  isLocalWorkbenchBaseUrl,
  normalizeLocalBaseUrl,
  probeWorkbenchBaseUrl,
  recoverLocalWorkbenchBaseUrl,
  waitForWorkbenchHealth,
};
